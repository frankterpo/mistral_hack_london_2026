"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.injectTComponentParameters = injectTComponentParameters;
const t = __importStar(require("@babel/types"));
const getObjectPropertyFromObjectExpression_1 = require("../../utils/parsing/getObjectPropertyFromObjectExpression");
const errors_1 = require("../../utils/errors");
/**
 * Inject parameters into a T component
 */
function injectTComponentParameters(callExpr, state) {
    // Increment counter (in place of registering the component)
    const identifier = state.stringCollector.incrementCounter();
    // Look up translationJsx
    const translationJsx = state.stringCollector.getTranslationJsx(identifier);
    if (!translationJsx) {
        return;
    }
    // Get second arg
    if (callExpr.arguments.length < 2 || !callExpr.arguments[1]) {
        state.logger.logError('T component jsx invocation is missing its second argument. Parameter injection failed.' +
            (0, errors_1.createErrorLocation)(callExpr));
        return;
    }
    if (!t.isObjectExpression(callExpr.arguments[1])) {
        state.logger.logError("T component jsx invocation's second argument is not an object expression. Parameter injection failed." +
            (0, errors_1.createErrorLocation)(callExpr));
        return;
    }
    // If already has hash set then skip
    if ((0, getObjectPropertyFromObjectExpression_1.getObjectPropertyFromObjectExpression)(callExpr.arguments[1], 'hash')) {
        return;
    }
    // Inject the parameters into the call expression
    callExpr.arguments[1].properties.push(t.objectProperty(t.identifier('_hash'), t.stringLiteral(translationJsx.hash)));
}
//# sourceMappingURL=injectTComponentParameters.js.map