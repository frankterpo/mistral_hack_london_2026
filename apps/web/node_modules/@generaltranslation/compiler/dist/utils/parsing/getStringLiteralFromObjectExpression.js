"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.getStringLiteralFromObjectExpression = getStringLiteralFromObjectExpression;
const t = __importStar(require("@babel/types"));
const validateExpressionIsStringLiteral_1 = require("../validation/validateExpressionIsStringLiteral");
const getStringLiteralFromExpression_1 = require("./getStringLiteralFromExpression");
/**
 * Given an expression, return the object property
 */
function getStringLiteralFromObjectExpression(objExpr, name) {
    for (const property of objExpr.properties) {
        // Skip spread elements and methods
        if (t.isSpreadElement(property) || t.isMethod(property)) {
            continue;
        }
        // Skip non-string literals
        // TODO: switch this over to error extraction format
        if (!t.isExpression(property.value) ||
            !(0, validateExpressionIsStringLiteral_1.validateExpressionIsStringLiteral)(property.value)) {
            continue;
        }
        // Skip non-matching keys
        if (t.isIdentifier(property.key) && property.key.name === name) {
            return (0, getStringLiteralFromExpression_1.getStringLiteralFromExpression)(property.value);
        }
        if (t.isStringLiteral(property.key) && property.key.value === name) {
            return (0, getStringLiteralFromExpression_1.getStringLiteralFromExpression)(property.value);
        }
    }
}
//# sourceMappingURL=getStringLiteralFromObjectExpression.js.map