"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.findParamIndexFromImplicitTags = findParamIndexFromImplicitTags;
/**
 * Given a list of implicit tags, returns the index of the parameter
 * @param implicitTags
 * @param param
 * @returns The index of the parameter (-1 if not found)
 * @example
 * ```ts
 * const implicitTags = ['_N_T_/[locale]/fr', '_N_T_/page/homepage'];
 * const index = findParamIndexFromImplicitTags(implicitTags, 'locale');
 * console.log(index); // 1
 * ```
 */
function findParamIndexFromImplicitTags(implicitTags, param) {
    var encodedParam = "[".concat(param, "]");
    var encodedTag = implicitTags.find(function (tag) { return tag.includes(encodedParam); });
    if (!encodedTag)
        return -1;
    // Filter out route groups from the pattern to match the actual URL structure
    // Route groups like (dashboard) exist in patterns but not in actual URLs
    var segments = encodedTag.split('/');
    var filteredSegments = segments.filter(function (tag) { return !tag.startsWith('(') && !tag.endsWith(')'); });
    return filteredSegments.indexOf(encodedParam);
}
//# sourceMappingURL=findParamIndexFromImplicitTags.js.map