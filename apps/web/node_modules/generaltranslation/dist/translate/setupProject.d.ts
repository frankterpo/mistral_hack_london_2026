export type SetupProjectResult = {
    setupJobId: string;
    status: 'queued';
} | {
    status: 'completed';
};
export type SetupProjectOptions = {
    force?: boolean;
    locales?: string[];
    timeoutMs?: number;
};
