declare class ApiError extends Error {
    code: number;
    message: string;
    constructor(error: string, code: number, message: string);
    getCode(): number;
    getMessage(): string;
}

export { ApiError };
