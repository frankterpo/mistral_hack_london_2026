"use strict";
var __assign = (this && this.__assign) || function () {
    __assign = Object.assign || function(t) {
        for (var s, i = 1, n = arguments.length; i < n; i++) {
            s = arguments[i];
            for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p))
                t[p] = s[p];
        }
        return t;
    };
    return __assign.apply(this, arguments);
};
var __awaiter = (this && this.__awaiter) || function (thisArg, _arguments, P, generator) {
    function adopt(value) { return value instanceof P ? value : new P(function (resolve) { resolve(value); }); }
    return new (P || (P = Promise))(function (resolve, reject) {
        function fulfilled(value) { try { step(generator.next(value)); } catch (e) { reject(e); } }
        function rejected(value) { try { step(generator["throw"](value)); } catch (e) { reject(e); } }
        function step(result) { result.done ? resolve(result.value) : adopt(result.value).then(fulfilled, rejected); }
        step((generator = generator.apply(thisArg, _arguments || [])).next());
    });
};
var __generator = (this && this.__generator) || function (thisArg, body) {
    var _ = { label: 0, sent: function() { if (t[0] & 1) throw t[1]; return t[1]; }, trys: [], ops: [] }, f, y, t, g = Object.create((typeof Iterator === "function" ? Iterator : Object).prototype);
    return g.next = verb(0), g["throw"] = verb(1), g["return"] = verb(2), typeof Symbol === "function" && (g[Symbol.iterator] = function() { return this; }), g;
    function verb(n) { return function (v) { return step([n, v]); }; }
    function step(op) {
        if (f) throw new TypeError("Generator is already executing.");
        while (g && (g = 0, op[0] && (_ = 0)), _) try {
            if (f = 1, y && (t = op[0] & 2 ? y["return"] : op[0] ? y["throw"] || ((t = y["return"]) && t.call(y), 0) : y.next) && !(t = t.call(y, op[1])).done) return t;
            if (y = 0, t) op = [op[0] & 2, t.value];
            switch (op[0]) {
                case 0: case 1: t = op; break;
                case 4: _.label++; return { value: op[1], done: false };
                case 5: _.label++; y = op[1]; op = [0]; continue;
                case 7: op = _.ops.pop(); _.trys.pop(); continue;
                default:
                    if (!(t = _.trys, t = t.length > 0 && t[t.length - 1]) && (op[0] === 6 || op[0] === 2)) { _ = 0; continue; }
                    if (op[0] === 3 && (!t || (op[1] > t[0] && op[1] < t[3]))) { _.label = op[1]; break; }
                    if (op[0] === 6 && _.label < t[1]) { _.label = t[1]; t = op; break; }
                    if (t && _.label < t[2]) { _.label = t[2]; _.ops.push(op); break; }
                    if (t[2]) _.ops.pop();
                    _.trys.pop(); continue;
            }
            op = body.call(thisArg, _);
        } catch (e) { op = [6, e]; y = 0; } finally { f = t = 0; }
        if (op[0] & 5) throw op[1]; return { value: op[0] ? op[1] : void 0, done: true };
    }
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.getTranslations = getTranslations;
exports.useTranslations = useTranslations;
var internal_1 = require("gt-react/internal");
var getDictionary_1 = __importDefault(require("../../dictionary/getDictionary"));
var createErrors_1 = require("../../errors/createErrors");
var getI18NConfig_1 = __importDefault(require("../../config-dir/getI18NConfig"));
var getLocale_1 = require("../../request/getLocale");
var id_1 = require("generaltranslation/id");
var use_1 = __importDefault(require("../../utils/use"));
var internal_2 = require("gt-react/internal");
var setDictionary_1 = __importDefault(require("../../dictionary/setDictionary"));
var internal_3 = require("generaltranslation/internal");
/**
 * Returns the dictionary access function t(), which is used to translate an item from the dictionary.
 *
 * @param {string} [id] - Optional prefix to prepend to the translation keys.
 * @returns {Function} A translation function that accepts a key string and returns the translated value.
 *
 * @example
 * const t = await getTranslations('user');
 * console.log(t('name')); // Translates item 'user.name'
 *
 * const t = await getTranslations();
 * console.log(t('hello')); // Translates item 'hello'
 */
function getTranslations(id) {
    return __awaiter(this, void 0, void 0, function () {
        var getId, dictionary, I18NConfig, locale, defaultLocale, translationRequired, gt, dictionaryTranslations, _a, translations, _b, t;
        return __generator(this, function (_c) {
            switch (_c.label) {
                case 0:
                    getId = function (suffix) {
                        return id ? "".concat(id, ".").concat(suffix) : suffix;
                    };
                    return [4 /*yield*/, (0, getDictionary_1.default)()];
                case 1:
                    dictionary = (_c.sent()) || {};
                    I18NConfig = (0, getI18NConfig_1.default)();
                    return [4 /*yield*/, (0, getLocale_1.getLocale)()];
                case 2:
                    locale = _c.sent();
                    defaultLocale = I18NConfig.getDefaultLocale();
                    translationRequired = I18NConfig.requiresTranslation(locale)[0];
                    gt = I18NConfig.getGTClass();
                    if (!translationRequired) return [3 /*break*/, 4];
                    return [4 /*yield*/, I18NConfig.getDictionaryTranslations(locale)];
                case 3:
                    _a = _c.sent();
                    return [3 /*break*/, 5];
                case 4:
                    _a = undefined;
                    _c.label = 5;
                case 5:
                    dictionaryTranslations = _a;
                    if (!translationRequired) return [3 /*break*/, 7];
                    return [4 /*yield*/, I18NConfig.getCachedTranslations(locale)];
                case 6:
                    _b = _c.sent();
                    return [3 /*break*/, 8];
                case 7:
                    _b = undefined;
                    _c.label = 8;
                case 8:
                    translations = _b;
                    t = function (id, options) {
                        if (options === void 0) { options = {}; }
                        // Get entry
                        id = getId(id);
                        var value = (0, internal_1.getDictionaryEntry)(dictionary, id);
                        // Check: no entry found
                        if (!value) {
                            console.warn((0, createErrors_1.createNoEntryFoundWarning)(id));
                            return '';
                        }
                        // Check: invalid entry
                        if (!(0, internal_1.isValidDictionaryEntry)(value)) {
                            console.warn((0, createErrors_1.createInvalidDictionaryEntryWarning)(id));
                            return '';
                        }
                        // Get entry and metadata
                        // eslint-disable-next-line prefer-const
                        var _a = (0, internal_1.getEntryAndMetadata)(value), entry = _a.entry, metadata = _a.metadata;
                        // Validate entry
                        if (!entry || typeof entry !== 'string')
                            return '';
                        // Render method
                        var renderContent = function (message, locales, fallback) {
                            var _a;
                            var _b, _c;
                            try {
                                // (1) Try to format message
                                var declaredVars = (0, internal_3.extractVars)(fallback || '');
                                var formattedMessage = gt.formatMessage(Object.keys(declaredVars).length ? (0, internal_3.condenseVars)(message) : message, {
                                    locales: locales,
                                    variables: __assign(__assign(__assign({}, options), declaredVars), (_a = {}, _a[internal_3.VAR_IDENTIFIER] = 'other', _a)),
                                });
                                var cutoffMessage = gt.formatCutoff(formattedMessage, {
                                    locales: locales,
                                    maxChars: (_b = metadata === null || metadata === void 0 ? void 0 : metadata.$maxChars) !== null && _b !== void 0 ? _b : options.$maxChars,
                                });
                                return cutoffMessage;
                            }
                            catch (error) {
                                if (process.env.NODE_ENV === 'production') {
                                    console.warn((0, createErrors_1.createInvalidIcuDictionaryEntryWarning)(id), 'Error: ', error);
                                }
                                else {
                                    // (3) If no fallback, throw error (non-prod)
                                    if (!fallback)
                                        throw new Error("".concat((0, createErrors_1.createInvalidIcuDictionaryEntryError)(id), " Error: ").concat(error));
                                    console.error((0, createErrors_1.createInvalidIcuDictionaryEntryError)(id), 'Error: ', error);
                                }
                                // (2) If format fails, format fallback
                                if (fallback) {
                                    return renderContent(fallback, locales);
                                }
                                // (3) Fallback to original message (unformatted)
                                var cutoffMessage = gt.formatCutoff(message, {
                                    locales: locales,
                                    maxChars: (_c = metadata === null || metadata === void 0 ? void 0 : metadata.$maxChars) !== null && _c !== void 0 ? _c : options.$maxChars,
                                });
                                return cutoffMessage; // fallback to original message (unformatted)}
                            }
                        };
                        // Check: translation required
                        if (!translationRequired)
                            return renderContent(entry, [defaultLocale]);
                        // ---------- DICTIONARY TRANSLATIONS ---------- //
                        // Get dictionaryTranslation
                        var dictionaryTranslation = (0, internal_1.getDictionaryEntry)(dictionaryTranslations || {}, id);
                        // Check: invalid entry
                        if (dictionaryTranslation !== undefined &&
                            (!(0, internal_1.isValidDictionaryEntry)(dictionaryTranslation) ||
                                typeof dictionaryTranslation !== 'string')) {
                            console.warn((0, createErrors_1.createInvalidDictionaryTranslationEntryWarning)(id));
                            return renderContent(entry, [defaultLocale]);
                        }
                        // Render dictionaryTranslation
                        if (dictionaryTranslation) {
                            return renderContent(dictionaryTranslation, [locale, defaultLocale], entry);
                        }
                        // ---------- TRANSLATION ---------- //
                        var translationEntry = translations === null || translations === void 0 ? void 0 : translations[id];
                        var hash = '';
                        var getHash = function () {
                            if (metadata === null || metadata === void 0 ? void 0 : metadata.$_hash)
                                return metadata.$_hash;
                            var hash = (0, id_1.hashSource)(__assign(__assign(__assign({ source: (0, internal_3.indexVars)(entry) }, ((metadata === null || metadata === void 0 ? void 0 : metadata.$context) && { context: metadata.$context })), ((metadata === null || metadata === void 0 ? void 0 : metadata.$maxChars) != null && {
                                maxChars: Math.abs(metadata.$maxChars),
                            })), { id: id, dataFormat: 'ICU' }));
                            // Inject hash if not there yet
                            metadata = __assign(__assign({}, metadata), { $_hash: hash });
                            (0, internal_1.injectEntry)([entry, metadata], dictionary, id, dictionary);
                            return hash;
                        };
                        if (!translationEntry) {
                            hash = getHash();
                            translationEntry = translations === null || translations === void 0 ? void 0 : translations[hash];
                        }
                        // ----- RENDER TRANSLATION ----- //
                        // If a translation already exists
                        if (translationEntry)
                            return renderContent(translationEntry, [locale, defaultLocale], entry);
                        // If a translation errored
                        if (translationEntry === null)
                            return renderContent(entry, [defaultLocale]);
                        // ----- CREATE TRANSLATION ----- //
                        // Since this is buildtime string translation, it's dev only
                        if (!I18NConfig.isDevelopmentApiEnabled()) {
                            console.warn((0, createErrors_1.createDictionaryTranslationError)(id));
                            return renderContent(entry, [defaultLocale]);
                        }
                        try {
                            // Translate on demand
                            I18NConfig.translateIcu({
                                source: (0, internal_3.indexVars)(entry),
                                targetLocale: locale,
                                options: __assign(__assign(__assign({}, ((metadata === null || metadata === void 0 ? void 0 : metadata.$context) && { context: metadata.$context })), ((metadata === null || metadata === void 0 ? void 0 : metadata.$maxChars) && { maxChars: metadata.$maxChars })), { id: id, hash: getHash() }),
                            }).then(function (result) {
                                // Log the translation result for debugging purposes
                                // eslint-disable-next-line no-console
                                console.warn((0, createErrors_1.createTranslationLoadingWarning)(__assign(__assign({}, (id && { id: id })), { source: renderContent(entry, [defaultLocale]), translation: renderContent(result, [
                                        locale,
                                        defaultLocale,
                                    ]) })));
                                // inject
                                (0, internal_1.injectEntry)(result, dictionaryTranslations, id, dictionary);
                            });
                        }
                        catch (error) {
                            console.warn(error);
                        }
                        // Default is returning source, rather than returning a loading state
                        return renderContent(entry, [defaultLocale]);
                    };
                    /**
                     * @description A function that translates a dictionary object and returns it
                     * @param id The identifier of the dictionary entry to translate.
                     * @param options The options for the dictionary entry (if applicable)
                     */
                    t.obj = function (id, options) {
                        if (options === void 0) { options = {}; }
                        // (1) Get subtree
                        var idWithParent = getId(id);
                        var subtree = (0, internal_2.getSubtree)({ dictionary: dictionary, id: idWithParent });
                        // Check: no subtree found
                        if (!subtree) {
                            console.warn((0, createErrors_1.createNoEntryFoundWarning)(idWithParent));
                            return {};
                        }
                        // Check: if subTreeTranslation is a dictionaryEntry
                        if ((0, internal_1.isDictionaryEntry)(subtree)) {
                            return t(id, options);
                        }
                        // Check: if is default locale
                        if (!translationRequired) {
                            // remove metadata from entries
                            var strippedSubtree = (0, internal_1.stripMetadataFromEntries)(subtree);
                            return strippedSubtree;
                        }
                        // Set up the dictionaryTranslations object if it doesn't exist
                        if (!dictionaryTranslations) {
                            dictionaryTranslations = {};
                            I18NConfig.setDictionaryTranslations(locale, dictionaryTranslations);
                        }
                        var translatedSubtree = (0, internal_1.getSubtreeWithCreation)({
                            dictionary: dictionaryTranslations,
                            id: idWithParent,
                            sourceDictionary: dictionaryTranslations,
                        });
                        // (2) Calculate subtreeWithHashes, dictionaryTranslationsWithTranslations, translatedSubtreeWithFallbacks, and untranslatedEntries
                        // Note: the following four operations can technically be combined into one traversal, but this
                        // strategy is much more readable and much easier to test/debug
                        // Inject hashes into subtree
                        var _a = (0, internal_1.injectHashes)(
                        // eslint-disable-next-line no-undef
                        structuredClone(subtree), idWithParent), subtreeWithHashes = _a.dictionary, updateDictionary = _a.updateDictionary;
                        // Collect untranslated entries
                        var untranslatedEntries = (0, internal_1.collectUntranslatedEntries)(subtreeWithHashes, translatedSubtree, idWithParent);
                        // Inject translations into translation subtree
                        var _b = (0, internal_1.injectTranslations)(subtreeWithHashes, 
                        // eslint-disable-next-line no-undef
                        structuredClone(translatedSubtree), translations || {}, untranslatedEntries, idWithParent), dictionaryTranslationsWithTranslations = _b.dictionary, updateDictionaryTranslations = _b.updateDictionary;
                        // Inject fallbacks into translation subtree
                        var translatedSubtreeWithFallbacks = (0, internal_1.injectFallbacks)(subtreeWithHashes, 
                        // eslint-disable-next-line no-undef
                        structuredClone(dictionaryTranslationsWithTranslations), untranslatedEntries, idWithParent);
                        var _loop_1 = function (untranslatedEntry) {
                            var source = untranslatedEntry.source, metadata = untranslatedEntry.metadata;
                            var id_2 = metadata === null || metadata === void 0 ? void 0 : metadata.$id;
                            // (3.a) Translate
                            I18NConfig.translateIcu({
                                source: (0, internal_3.indexVars)(source),
                                targetLocale: locale,
                                options: __assign(__assign(__assign({}, ((metadata === null || metadata === void 0 ? void 0 : metadata.$context) && { context: metadata.$context })), ((metadata === null || metadata === void 0 ? void 0 : metadata.$maxChars) && { maxChars: metadata.$maxChars })), { id: id_2, hash: metadata === null || metadata === void 0 ? void 0 : metadata.$_hash }),
                            })
                                // (3.b) Inject the translation into the translations object
                                .then(function (result) {
                                (0, internal_1.injectEntry)(result, dictionaryTranslations, id_2, dictionary);
                            });
                        };
                        // (3) For each untranslated entry, translate it
                        for (var _i = 0, untranslatedEntries_1 = untranslatedEntries; _i < untranslatedEntries_1.length; _i++) {
                            var untranslatedEntry = untranslatedEntries_1[_i];
                            _loop_1(untranslatedEntry);
                        }
                        // (5) Update the dictionaryTranslations object and dictionary
                        // inject translatedSubtreeWithFallbacks and new subtree objects
                        if (updateDictionary) {
                            var newDictionary = (0, internal_1.injectAndMerge)(dictionary, subtreeWithHashes, idWithParent);
                            (0, setDictionary_1.default)(newDictionary);
                        }
                        if (updateDictionaryTranslations) {
                            var newDictionaryTranslations = (0, internal_1.mergeDictionaries)(dictionaryTranslations, dictionaryTranslationsWithTranslations);
                            I18NConfig.setDictionaryTranslations(locale, newDictionaryTranslations);
                        }
                        // (4) Copy the dictionaryTranslations object
                        // eslint-disable-next-line no-undef
                        return structuredClone(translatedSubtreeWithFallbacks);
                    };
                    return [2 /*return*/, t];
            }
        });
    });
}
/**
 * Returns the dictionary access function t(), which is used to translate an item from the dictionary.
 *
 * @param {string} [id] - Optional prefix to prepend to the translation keys.
 * @returns {Function} A translation function that accepts a key string and returns the translated value.
 *
 * @example
 * const t = useTranslations('user');
 * console.log(t('name')); // Translates item 'user.name'
 *
 * const t = useTranslations();
 * console.log(t('hello')); // Translates item 'hello'
 */
function useTranslations(id) {
    return (0, use_1.default)(getTranslations(id));
}
//# sourceMappingURL=getTranslations.js.map